export enum StateType {
  EAT,
  REST,
  RUN,
  HOMEWORK
}

export enum ParameterType {
  NUMBER,
  BOOLEAN,
  STRING,
}

export enum ParameterCompare {
  INVALID,
  GREATER,
  LESS,
  EQUAL,
  NOT_EQUAL,
  GREATER_EQUAL,
  LESS_EQUAL,
}